<%= simple_form_for([:admin, ride]) do |f| %>
      <%= f.input :passengers, label: 'Number of Passengers'%>
      <%= f.input :departure_time, :as => :datetime_picker, :label => 'Departure Time'%>
      <%= f.input :destination %>

    <%= f.button :submit, class: "btn-primary" %>
<% end %>